export type MenuItemProps = {
  image: string;
  title: string;
  price: number;
  id: number;
  quantity: number;
  category: string;
};

export type MenuItem = MenuItemProps;
export type MenuItems = MenuItem[];
