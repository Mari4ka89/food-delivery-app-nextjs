// TODO: extract to env variable
/*const API_URL = "https://dungarees-crow.cyclic.app/api/";

export const getVendorsData = async () => {
  try {
    let response = await fetch(`${API_URL}products/categories`);
    let vendors = await response.json();

    return vendors;
  } catch (e) {
    // TODO: show more informative error messages using e.status
    console.log("Error occured while fetching vendors");

    return [];
  }
};*/
